{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "build:sd": "turbo run build:sd"
  },
  "devDependencies": {
    "turbo": "^1.8.3"
  }
}
